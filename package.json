{
  "name": "my-first-devin-project",
  "scripts": {
    "lint": "pnpm biome check --write src",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.7.8"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "vitest": "^3.1.2",
    "wrangler": "^4.4.0"
  }
}